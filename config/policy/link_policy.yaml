# USER-MANAGED POLICY FILE. SAFE TO EDIT.
# Purpose: define allowed hierarchy/link semantics for canonical bundles and writes.
# Team governance rules for ADO links.
schema_version: "1.0"
allowed_link_types:
  - parent-child
max_depth: 2
forbid_double_nesting:
  - KeyResult
  - Feature
  - UserStory
